 <%- if user_signed_in? %>
<h1>New Game</h1>

<%= render 'form', game: @game %>

<%= link_to 'Back', games_path,  class: "btn btn-info outline" %>
<% else %>
<h1>Sign in to acces this page</h1>
<% end %>